apiVersion: v1
kind: Secret
metadata:
  name: ptyxiakh-secret
type: Opaque
stringData:
  # No API keys needed — the stack uses local Ollama + local embeddings.
  # Add any future secrets here (e.g. if you add auth to the Gradio UI).
  PLACEHOLDER: "replace-if-needed"
